const assert = require('assert');
const BranchControllerSpec = require('../branch.controller.spec.ts');

describe('BranchControllerSpec', () => {
  it('has a test', () => {
    assert(false, 'BranchControllerSpec should have a test');
  });
});

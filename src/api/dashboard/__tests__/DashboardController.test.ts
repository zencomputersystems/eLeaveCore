const assert = require('assert');
const DashboardController = require('../dashboard.controller.ts');

describe('DashboardController', () => {
  it('has a test', () => {
    assert(false, 'DashboardController should have a test');
  });
});

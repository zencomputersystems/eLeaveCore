const assert = require('assert');
const AuthController = require('../auth.controller.ts');

describe('AuthController', () => {
  it('has a test', () => {
    assert(false, 'AuthController should have a test');
  });
});

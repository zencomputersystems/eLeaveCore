const assert = require('assert');
const DashboardModule = require('../dashboard.module.ts');

describe('DashboardModule', () => {
  it('has a test', () => {
    assert(false, 'DashboardModule should have a test');
  });
});
